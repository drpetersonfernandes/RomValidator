<Window x:Class="RomValidator.DuplicateFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Duplicate Files Detected"
        Height="600"
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="SingleBorderWindow">
    

    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Duplicate Files Detected" Style="{StaticResource DuplicateWindowHeaderTextBlockStyle}" />
        
        <Border Grid.Row="1" Background="{StaticResource DuplicateWarningBackgroundBrush}" BorderBrush="{StaticResource DuplicateWarningBorderBrush}" BorderThickness="1" CornerRadius="3" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Warning: Duplicate ROMs detected in the input folder!" FontWeight="Bold" Foreground="{StaticResource DuplicateWarningTextBrush}" />
                <TextBlock Text="These are the same ROM data stored under different filenames." Margin="0,5,0,0" />
                <TextBlock Text="Consider removing duplicates before exporting the DAT file." Margin="0,2,0,0" />
            </StackPanel>
        </Border>
        
        <Border Grid.Row="2" BorderBrush="{StaticResource StatusBarGrayBrush}" BorderThickness="1" CornerRadius="3" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="{StaticResource LightBackgroundBrush}" BorderBrush="{StaticResource StatusBarGrayBrush}" BorderThickness="0,0,0,1" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="Duplicate Groups:" Style="{StaticResource DuplicateWindowLabelTextBlockStyle}" />
                        <TextBlock Grid.Column="1" Text=" (same SHA256 hash, different filenames)" Margin="5,0,0,0" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="2" Name="DuplicateCountText" FontWeight="Bold" VerticalAlignment="Center" />
                    </Grid>
                </Border>
                
                <ListView Grid.Row="1" Name="DuplicateListView" BorderThickness="0"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="SHA256 Hash" DisplayMemberBinding="{Binding Hash}" Width="280"/>
                            <GridViewColumn Header="Filenames" DisplayMemberBinding="{Binding Filenames}" Width="400"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
        
        <Button Grid.Row="3" Name="CloseButton" Content="Close" Style="{StaticResource DuplicateWindowCloseButtonStyle}" 
                HorizontalAlignment="Right" Width="100" Click="CloseButton_Click" />
    </Grid>
</Window>