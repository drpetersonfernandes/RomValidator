<Window x:Class="RomValidator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ROM Validator" Height="720" Width="1200" MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Background" Value="#2979FF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E88E5" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with application title and navigation buttons -->
        <Border Grid.Row="0" Background="#1976D2" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="ROM Validator" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,30,0" />
                    <Button Content="Validate ROMs" Click="ValidateRoms_Click" Style="{StaticResource MenuButtonStyle}" ToolTip="Validate ROMs against a DAT file" />
                    <Button Content="Generate DAT" Click="GenerateDat_Click" Style="{StaticResource MenuButtonStyle}" ToolTip="Generate a DAT file from a folder of ROMs" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="About" Click="About_Click" Style="{StaticResource MenuButtonStyle}" Background="#9C27B0" ToolTip="View application information" />
                    <Button Content="Exit" Click="Exit_Click" Style="{StaticResource MenuButtonStyle}" Background="#607D8B" ToolTip="Close the application" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content frame -->
        <Frame Grid.Row="1"
               Name="MainContentFrame"
               NavigationUIVisibility="Hidden"
               Background="#F5F5F5" />

        <!-- Status bar -->
        <StatusBar Grid.Row="2" Background="#E0E0E0" Padding="5">
            <StatusBarItem>
                <TextBlock x:Name="StatusBarMessageTextBlock" Text="Ready." />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
